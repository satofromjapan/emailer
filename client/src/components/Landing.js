import React from 'react';

const Landing = () => {
	return (
		<div style={{ textAlign: 'center' }}>
			<h1>Emailr</h1>
			<p>Do you need feedback from users but don't have the time?</p>
			<p>Use Emailr to do it one simple step!</p>
		</div>
	);
};

export default Landing;
